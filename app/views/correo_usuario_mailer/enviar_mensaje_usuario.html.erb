<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <style>
      body {
        font-family: Arial, sans-serif;
        font-size: 14px;
        color: #333;
        background-color: #fff;
        line-height: 1.5;
      }
      .contenido {
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 8px;
        background-color: #f9f9f9;
      }
      .etiqueta {
        font-weight: bold;
        color: #222;
      }
    </style>
  </head>
  <body>
    <div class="contenido">
      <p class="etiqueta">Remitente:</p>
      <p><%= @correo_usuario.nombre.presence || 'Anónimo' %></p>

      <p class="etiqueta">Correo Electrónico:</p>
      <p><%= @correo_usuario.remitente %></p>

      <p class="etiqueta">Asunto:</p>
      <p><%= @correo_usuario.asunto %></p>

      <p class="etiqueta">Mensaje:</p>
      <p><%= simple_format(@correo_usuario.cuerpo) %></p>

      <p style="font-family: 'Brush Script MT', cursive; font-size: 24px; color: #2c3e50; margin-top: 40px;">
        Infomóvil
      </p>
    </div>
    <!-- Enlace para cancelar suscripción -->
    <p style="font-size: 12px; color: #777; margin-top: 30px;">
      ¿No deseas recibir más correos? 
      <a href="<%= unsubscribe_url(@correo_usuario.destinatario) %>" style="color: #555;">
        Haz clic aquí para cancelar la suscripción
      </a>.
    </p>
  </body>
</html>
